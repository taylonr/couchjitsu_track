<%= for event <- events @conn do %>
  <div class="ui feed">
    <h2 class="ui center aligned dividing header">
      <%= event.date %>
    </h2>
    <%= for item <- events event do %>
      <%= render "event.html", e: item %>
    <% end %>
  </div>
<% end %>